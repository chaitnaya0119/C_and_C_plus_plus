#include<stdio.h>
int main()
{
    int a,b,mult;
    printf("enter the value of a and b:",a,b);
    scanf("%d,%d",&a,&b);
    mult=a*b;
    printf("the value of mult is %d",mult);
    return 0;

}
